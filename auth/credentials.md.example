# ██████ PROJECT SILHOUETTE — CREDENTIALS REFERENCE ██████
# TEMPLATE ONLY — Copy to credentials.md and fill in your values.
# credentials.md is in .gitignore and MUST NEVER be committed.

---

## Auth0 Configuration (Frontend SPA Only)

AUTH0_DOMAIN:    (From Auth0 Application settings, e.g. dev-xxxx.us.auth0.com)
AUTH0_CLIENT_ID: (From Auth0 Application settings)

### API Backend (Optional)
AUTH0_AUDIENCE:  (Leave blank for frontend-only SPA. Only set this if you create a backend API in Auth0 later.)

---

## Backend URL (Optional)

Deployed Backend: https://YOUR_WORKER.YOUR_SUBDOMAIN.workers.dev

Endpoints:
  POST /api/track  — visitor tracking
  GET  /api/data   — fetch data (Requires Auth0 Audience + Token)

---

## Note Service (Optional Backend Support)

Using: google-tasks

---

## Google Tasks OAuth Credentials (Optional)

Client ID:      (from Google Cloud Console)
Client Secret:  (from Google Cloud Console)
Refresh Token:  (from OAuth Playground)
Task List ID:   (from Tasks API)

---

## Firebase Firestore (Optional Backend Support)

Project ID:      (from Firebase Console)
Client Email:    (from Service Account, ends in iam.gserviceaccount.com)
Private Key:     (download from Service Account as JSON, convert \n to actual newlines)

---

## Generated Date: (fill in)

---

# ██████ DO NOT COMMIT THIS FILE — ADD auth/ TO .gitignore ██████
